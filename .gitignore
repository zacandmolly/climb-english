node_modules/
dist/
dist 2/
.env
test-results/
playwright-report/
workers/wrangler.toml
qa-*.png
tsconfig.tsbuildinfo
.DS_Store
.workbuddy/
# 大媒体文件不入库（GitHub 100MB 单文件硬限制）。已追踪的历史媒体保留；
# 新素材媒体仅存本地 public/media/，可经导入管线重新下载生成。
public/media/*.mp4

# R10 runtime artifacts: the error inbox is appended by the dev server and the
# dated report is regenerated by `npm run errors:report`. Both are transient
# and should never be committed.
docs/error-inbox.jsonl
docs/error-report-*.md
