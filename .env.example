OPENAI_API_KEY=
OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini-transcribe
OPENAI_FEEDBACK_MODEL=gpt-4.1-mini
# Leave empty for explicit offline mode on static hosting; only use a stable HTTPS API.
VITE_FEEDBACK_API_BASE=
# Optional full collection endpoint. Empty in production means local buffering only.
VITE_ERROR_REPORT_ENDPOINT=
PORT=5173
